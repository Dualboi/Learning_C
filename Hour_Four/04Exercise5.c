/* 04 Exercises */
#include <stdio.h>
#include <string.h>

/* Exercise 4: Write a program that can print out the numeric format of the newline character \n. */
int main(void)
{
    char newLine = '\n';
    printf("%d", newLine);
    return 0;
}